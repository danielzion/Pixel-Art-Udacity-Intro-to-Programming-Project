let rows = 16;
let cols = 16;

function makeGrid(rows, cols) {
    for (let i = 0; i < rows; i++) {
        for (let j = 0; j < cols; j++) {
            console.log(`${i}.${j}`)
            
        }
    }
    
}

makeGrid(rows, cols)